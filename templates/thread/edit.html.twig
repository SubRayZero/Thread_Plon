{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	<header>
		<nav class="navbar navbar-expand-lg bg-body-tertiary">
			<div class="container-fluid">
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNav">
					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link" href="/home">Home</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_profil_thread') }}">Profil</a>
						</li>
					</ul>
				</div>
			</div>
		</nav>
	</header>

	{{ form_start(form) }}
	{{ form_errors(form) }}
	<div class="container">
		<div class="example-wrapper">
			<h1>Edit Thread</h1>
			<div class="mb-3">
				{{ form_errors(form.title) }}
				<label for="exampleInputText1" class="form-label">Title</label>
				{{ form_widget(form.title, {'attr': {'class': 'form-control', 'id': 'exampleInputText1'}}) }}
			</div>

			<div class="mb-3">
				{{ form_errors(form.description) }}
				<label for="exampleInputText1" class="form-label">Description</label>
				{{ form_widget(form.description, {'attr': {'class': 'form-control', 'id': 'exampleInputText1'}}) }}
			</div>

			<div class="mb-3">
				{{ form_errors(form.body) }}
				<label for="exampleFormControlTextarea1" class="form-label">Body</label>
				{{ form_widget(form.body, {'attr':{'class': 'form-control', 'id': 'exampleFormControlTextarea1', 'rows' : '3'}}
				)}}
			</div>

			<div class="mb-3 form-check">
				<label class="form-check-label" for="exampleCheck1">Category</label>
				{{ form_widget(form.category_id, {'id': 'exampleCheck1', 'class': 'form-check-input'}) }}
			</div>
			<button type="submit" class="btn btn-primary">Validation</button>
		</div>
	</div>
	{{ form_end(form) }}


{% endblock %}
